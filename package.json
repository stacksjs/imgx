{
  "name": "imgx",
  "type": "module",
  "version": "0.1.7",
  "private": true,
  "description": "Modern image optimizations & manipulations.",
  "author": "Chris Breuer <chris@stacksjs.org>",
  "license": "MIT",
  "homepage": "https://github.com/stacksjs/ts-images",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stacksjs/ts-images.git"
  },
  "bugs": {
    "url": "https://github.com/stacksjs/ts-images/issues"
  },
  "keywords": [
    "image",
    "manipulation",
    "compression",
    "web optimization",
    "jpg",
    "png",
    "avif",
    "webp",
    "sharp",
    "bun",
    "stacks",
    "typescript",
    "javascript"
  ],
  "scripts": {
    "fresh": "bunx rimraf node_modules/ bun.lock && bun i",
    "prepublishOnly": "bun --bun run build",
    "test": "bun test",
    "lint": "bunx --bun pickier .",
    "lint:fix": "bunx --bun pickier . --fix",
    "changelog": "bunx logsmith --verbose",
    "changelog:generate": "bunx logsmith --output CHANGELOG.md",
    "release": "bun --bun run changelog:generate && bunx --bun bumpx prompt --recursive",
    "postinstall": "bunx git-hooks",
    "typecheck": "bunx tsc --noEmit",
    "dev:docs": "bun --bun bunpress dev docs",
    "deploy:docs": "bun --bun bunpress deploy docs",
    "build:docs": "bun --bun bunpress build docs",
    "preview:docs": "bun --bun bunpress preview docs",
    "format": "bunx --bun pickier . --format",
    "format:fix": "bunx --bun pickier . --format --write"
  },
  "devDependencies": {
    "better-dx": "^0.2.7"
  },
  "workspaces": [
    "packages/*"
  ],
  "git-hooks": {
    "pre-commit": {
      "stagedLint": {
        "*.{js,ts,json,yaml,yml,md}": "bunx --bun pickier --fix"
      },
      "autoRestage": true
    },
    "commit-msg": "bunx --bun @stacksjs/gitlint --edit .git/COMMIT_EDITMSG"
  }
}
